[package]
name = "rust-webrtc-ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
webrtc = { path = "../rust-webrtc" }
tokio = { version = "1", features = ["full"] }
lazy_static = "1.4.0"

[build-dependencies]
cbindgen = "0.26.0"
